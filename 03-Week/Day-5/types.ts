/*

interface Product {
  id: number,
  name: string,
  price: number
};

function getProduct(id: number): Product {
  return {
  id: id,
  name: `Awesome Gadget ${id}`,
  price: 99.5
  }
}

const showProduct = (name: string, price: number) => {
  console.log(`The product ${name} costs ${price}$.`);
};

let age: number = 22;
let name: string = 'mertcan';
let isStudent: boolean = true;

function greet(name: string): string {
  return `Hello, ${name}`;
}

console.log(greet('mertcan')); // Hello, mertcan

let numbers: number[] = [1, 2, 3, 4, 5];
let names: string[] = ['Green', 'Red', 'Blue'];

let id: number | string;
id = 101;    // Valid
id = "101";  // Valid

interface User {
  name: string;
  age: number;
  isAdmin?: boolean; // Optional property
}

const user: User = {
  name: 'Alice',
  age: 30
};

enum Role {
  Admin,
  User,
  Guest
}

let userRole: Role = Role.User;

*/
